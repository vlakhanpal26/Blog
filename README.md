# Morning Pages Blog

Editorial personal blog built with Next.js App Router, Tailwind CSS, and MDX.

## Run locally

1. Install dependencies
   - `npm install`
2. Start dev server
   - `npm run dev`
3. Open `http://localhost:3000`

## Content

Posts live in `content/posts/*.mdx` and are loaded via frontmatter + MDX body rendering.
